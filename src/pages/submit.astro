---
import Layout from '../layouts/Layout.astro';



---

<Layout title="This Project is In Development by Jon-Factor">
	<main>
		<h1>Thank you for showing <span class="text-gradient"><br/>Intrest</span> in <span class="text-gradient">[in dev project name]</span></h1>
		<h1>You are being sent an <span class="text-gradient">introductory email,</span> as we speak</h1>	
		<div class=" img-contain">
			<img src="../../public/hooray-peanuts.gif" width="600" />
		</div>
	</main>
</Layout>
<script
type="text/javascript"
src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"
></script>
<script type="text/javascript">
(function () {
  emailjs.init("J01QMSmqj93QY8gmJ");
})();
</script>
<script>
	// onsubmit
	window.onload = () => {
		// grab email
		const params = new URLSearchParams(window.location.search)
		const email = params.get('email')

		// email validation
		var incorectEmail = false
		if (email === "" || email === null) {
			incorectEmail = true
		}
		if (incorectEmail) {
			console.log("Invalid Email")
			return
		}

		// send email

		const url = "https://api.emailjs.com/api/v1.0/email/send"
		// const dataz = {
		// service_id: "service_4y7bhci",
		// user_id: "bS0Wum3T74dBTaKlj",
		// template_id: "template_uq67gfl",
		// template_params: {
		// 	"to_name": email
		// 	}
		// }

		emailjs.send("service_4y7bhci", "template_uq67gfl", {"to_name":name})

		// const headerz = {
		// 	contentType:"application/json"
		// }

		// let paramz: RequestInit = {body:JSON.stringify(dataz), method: "POST", headers:headerz}

		// fetch(url, paramz).then((response) => {
		// 	console.log(response.body)
		// 	if (response.status === 200) {
		// 		console.log("Sucess!!!")
		// 	}
		// }).catch((response) => {
		// 	console.log(response.json())
		// })
	}
	
</script>

<style>
	.img-contain {
		display: flex;
		align-items: center;
		justify-content: center;
	}
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	.join {
		background:white;
		padding-left: 20px;
		display:flex;
		border-radius: 20px;
		overflow: hidden;
	}
	.input {
		height: 50px;
		font-size: 25px;
		width: 75%;
	}
	.submit {
		width: 25%
	}
	.astro-a {
		position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
		color:rgb(255, 251, 226) 
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	h2 {
		color: white;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		padding: 1.5rem;
		border-radius: 8px;
	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}
</style>